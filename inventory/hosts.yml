---
all:
  children:
    arp_targets:
      hosts:
      # Add real servers above, for example:
      #   server01:
      #     ansible_host: 192.0.2.20
      vars:
        # Undefined, null, or [] skips the host before opening SSH.
        # Set per host when interface names differ.
        # arp_interface: []
        # arp_interface:
        #   - eth0
        #   - eth1
        arp_interface:
          - ens18
